import type { Client } from './types';
export declare const connect: (url?: string) => Promise<Client>;
declare const _default: {
    connect: (url?: string) => Promise<Client>;
};
export default _default;
